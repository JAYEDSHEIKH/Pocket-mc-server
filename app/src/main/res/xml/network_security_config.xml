<?xml version="1.0" encoding="utf-8"?>
<!--
  Allow cleartext traffic ONLY to localhost (for RCON connections in future phases).
  All external API calls (PaperMC, Modrinth, etc.) use HTTPS — no cleartext needed.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow plaintext to loopback — needed for Source RCON (Phase 2) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
